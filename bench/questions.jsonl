{"prompt": "Write a Python function that parses an ISO 8601 date string with an optional timezone offset and returns a timezone-aware datetime. Include three test cases and explain the edge cases you handled."}
{"prompt": "Explain why memory bandwidth, not arithmetic throughput, limits large language model inference on consumer hardware. Use concrete numbers for a 12 GB GPU and a desktop CPU with 64 GB of RAM."}
{"prompt": "A lighthouse keeper finds a letter in a bottle. Write the first 400 words of the story, in the past tense, without dialogue."}
{"prompt": "Review this function and list every bug, then rewrite it correctly:\n\ndef median(xs):\n    xs.sort()\n    n = len(xs)\n    if n % 2 == 0:\n        return xs[n / 2]\n    return (xs[n // 2] + xs[n // 2 + 1]) / 2"}
{"prompt": "A train leaves a station at 09:10 travelling at 84 km/h. A second train leaves the same station at 09:40 on the same track at 112 km/h. At what time does the second train catch the first, and how far from the station? Show every step and check the answer two ways."}
{"prompt": "Extract every person, organisation, place and date from the passage below into a JSON object with those four keys, each a list of strings in order of first appearance, then explain any ambiguity you resolved:\n\nOn 3 March 1987 Dr. Amara Osei of the Kumasi Institute presented her findings to the Royal Society in London, where Sir Ian Hallett, then chair of its physics committee, invited her to repeat the work at the Rutherford laboratory near Oxford the following spring."}
{"prompt": "Write step-by-step instructions for replacing a bicycle's rear brake cable, for someone who has never done it. Name the tools, say what can go wrong at each step, and how to test the result."}
{"prompt": "Compare SQLite, PostgreSQL and DuckDB for a single-user desktop application that analyses a few gigabytes of CSV logs. Give a table of the trade-offs, then a recommendation with the two conditions under which you would change it."}
